import { SortedSitesPipe } from './sorted-sites.pipe';

describe('SortedSitesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortedSitesPipe();
    expect(pipe).toBeTruthy();
  });
});
